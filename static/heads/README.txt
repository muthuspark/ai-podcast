# Drop one avatar per speaker here as 0.glb, 1.glb, 2.glb, 3.glb
# Must PASS tools/check_glb.py (needs morph-target blendshapes incl. a mouth-open morph).
# Empty slots fall back to the bundled facecap head.
